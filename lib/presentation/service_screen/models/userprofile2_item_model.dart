import '../../../core/app_export.dart';/// This class is used in the [userprofile2_item_widget] screen.
class Userprofile2ItemModel {Userprofile2ItemModel({this.serviceName, this.plusText, this.addText, this.id, }) { serviceName = serviceName  ?? "Peroidic Services";plusText = plusText  ?? "+";addText = addText  ?? "ADD";id = id  ?? ""; }

String? serviceName;

String? plusText;

String? addText;

String? id;

 }
